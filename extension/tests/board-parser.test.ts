import { describe, it, expect } from "vitest";

// Tests for the DOM board parser.
// Will be populated once fixture HTML files are provided and
// the parser implementation is written.

describe("board parser", () => {
  it.todo("parses beginner board from fixture HTML");
  it.todo("parses intermediate board from fixture HTML");
  it.todo("parses expert board from fixture HTML");
  it.todo("extracts correct row and col count");
  it.todo("extracts mine count from counter display");
  it.todo("identifies all cell states correctly");
  it.todo("extracts post-game stats (time, 3BV, 3BV/s, IOE, clicks)");
});
